name: Windows CI Test

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.8
      uses: actions/setup-python@v2
      with:
        python-version: 3.8

    - name: install
      run: |
        pip install conan
        conan user

    - name: prepare
      run: |
        mkdir build
        dir
        cd build
        conan install ..

    - name: configure
      run: cmake .. -DCMAKE_BUILD_TYPE=Debug 

    - name: build
      run: make CppTemplate_unit_tests

    - name: test
      run: |
        ctest --build-config Debug
