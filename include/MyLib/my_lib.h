#pragma once

/**
 * @brief Prints hello world to the console.
 * 
 */
bool print_hello_world();

/**
 * @brief Creates a 3d vector and prints its elements to the console.
 * 
 */
bool print_linalg_vector();

/**
 * @brief Prints the current installed boost version.
 * 
 */
bool print_boost_version();
